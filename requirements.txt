# Infinity Outreach Agent — runtime dependencies
# Python 3.11+

# CLI
typer>=0.12.0
rich>=13.7.0

# Config / env
python-dotenv>=1.0.1
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Database
SQLAlchemy>=2.0.25

# HTTP + scraping/enrichment
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.1.0
# Trust the OS certificate store so corporate / antivirus TLS interception
# (common on Windows) doesn't break outbound HTTPS to OSM, Google and SMTP.
truststore>=0.9.0

# Local LLM (Ollama, OpenAI-compatible API)
openai>=1.30.0

# Optional CSV handling
pandas>=2.2.0

# Web frontend (local control panel)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.3
python-multipart>=0.0.9

# Dev / tests
pytest>=8.0.0
